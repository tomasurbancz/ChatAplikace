<UserControl x:Class="ChatAplikace.WPF.ChatView.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ChatAplikace.WPF.ChatView"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="LightGreen">
        
        <Grid x:Name="LeftBar" Width="240" HorizontalAlignment="Left">
            
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Hidden">
                <ItemsControl ItemsSource="{Binding Chats}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding Name}"
                                    Command="{Binding OpenCommand}"
                                    Margin="0"
                                    Height="50" Background="Green"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
        <Grid Name="ChatBar" Width="560" HorizontalAlignment="Right">
            <Grid Name="ChatHeader" Width="560" Height="70" VerticalAlignment="Top">
                <TextBlock Text="{Binding ChatUsername}"></TextBlock>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
